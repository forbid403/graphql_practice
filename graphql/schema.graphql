type Movie {
    title : String!
    rating : Float!
    id : Int!
    summary : String
    language : String
    medium_cover_image : String!
}

type Query {
    movies(limit: Int, rating: Float): [Movie]!
}
